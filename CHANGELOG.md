# Changelog

## [Unreleased]

### Added

- ACP/LLM bypass for OpenCodeBridge: support Claude Code as experiment code generation backend in repair and code generation stages (`llm` parameter passthrough, `_invoke_acp`, `_extract_response_files`)
- Research config `config.dinov2-rtdetr-visdrone.yaml` for DINOv2 backbone + RT-DETRv2 on VisDrone2019-DET aerial detection
- `CLAUDE.md` with repository guidance for AI assistants
